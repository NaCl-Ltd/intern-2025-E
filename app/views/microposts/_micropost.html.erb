<li id="micropost-<%= micropost.id %>">
  <%= link_to gravatar_for(micropost.user, size: 50), micropost.user %>
  <span class="user"><%= link_to micropost.user.name, micropost.user %></span>
  <span class="content">
    <%= micropost.content %>
    <% if micropost.image.attached? %>
      <%= image_tag micropost.image.variant(:display) %>
    <% end %>
  </span>
  <span class="timestamp">
    <% if micropost.pinned? %>
      📌 <strong>Pinned</strong> •
    <% end %>

    Posted <%= time_ago_in_words(micropost.created_at) %> ago.

    <!-- show pin/unpin buttons only for post owner -->
    <% if micropost.can_be_pinned_by?(current_user) %>
      <% if micropost.pinned? %>
        <%= button_to "Unpin", unpin_micropost_path(micropost), 
                      method: :patch, 
                      data: { confirm: "Unpin this post?" },
                      class: "btn btn-sm btn-outline-secondary" %>
      <% else %>
        <%= button_to "Pin", pin_micropost_path(micropost), 
                      method: :patch, 
                      data: { confirm: "Pin this post to the top?" },
                      class: "btn btn-sm btn-outline-primary" %>
      <% end %>
    <% end %>

    <% if current_user?(micropost.user) %>
      <%= link_to "delete", micropost, data: { "turbo-method": :delete,
                                               turbo_confirm: "You sure?" } %>
    <% end %>
  </span>
</li>
